#include <algorithm>
#include <iostream>

// One pass, three variables: position, high (seeded at 0 — the start counts),
// and a counter of steps that end exactly at 0. Update position first.

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int n;
    std::cin >> n;
    // TODO
    return 0;
}
