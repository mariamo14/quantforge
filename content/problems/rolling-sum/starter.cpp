#include <iostream>
#include <string>
#include <vector>

// Keep one running sum: add the arriving value; once the window is full,
// subtract the value that just fell out (v[i-K]). Print after every step.

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int n, k;
    std::cin >> n >> k;
    // TODO
    return 0;
}
